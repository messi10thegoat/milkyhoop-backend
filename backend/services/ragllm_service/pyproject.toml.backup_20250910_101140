[tool.poetry]
name = "ragllm_service"
version = "0.1.0"
description = "MilkyHoop RAG LLM Service - Direct Dependencies Only"
authors = ["MilkyHoop Team"]
packages = [{ include = "app" }]

[tool.poetry.dependencies]
python = "^3.10"

# Core Web Framework
fastapi = "^0.110.0"
uvicorn = {extras = ["standard"], version = "^0.29.0"}

# gRPC Communication
grpcio = "^1.63.0"
grpcio-tools = "^1.63.0"
grpcio-health-checking = "^1.63.0"
grpc-interceptor = "^0.15.0"
protobuf = "^5.29.0"

# LLM Integration
openai = "^1.0.0"

# Scientific Computing
numpy = "^1.26.0"
scikit-learn = "^1.3.0"

# Vector Search
faiss-cpu = "^1.11.0"

# Text Processing
fuzzywuzzy = "^0.18.0"

# Database & ORM
prisma = "^0.15.0"

# HTTP Client
httpx = "^0.27.0"

# Logging & Configuration
structlog = "^24.1.0"
python-dotenv = "^1.0.0"

# Utilities
tenacity = "^8.2.0"

[tool.poetry.dev-dependencies]
pytest = "^8.1.1"
mypy = "^1.9.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
