[tool.poetry]
name = "auth_service"
version = "0.1.0"
description = "MilkyHoop Authentication Service - Direct Dependencies Only"
authors = ["MilkyHoop Team"]

[tool.poetry.dependencies]
python = "^3.10"

# Core Web Framework
fastapi = "^0.110.0"
uvicorn = {extras = ["standard"], version = "^0.29.0"}

# gRPC Communication
grpcio = "^1.74.0"
grpcio-tools = "^1.63.0"
grpcio-health-checking = "^1.63.0"
grpc-interceptor = "^0.15.0"
protobuf = "^5.29.0"

# Database & ORM
prisma = "0.15.0"

# Authentication & Security
pyjwt = "^2.8.0"
bcrypt = "^4.0.0"
passlib = "^1.7.0"

# HTTP Client
httpx = "^0.27.0"

# Logging & Configuration
structlog = "^24.1.0"
python-dotenv = "^1.0.0"

[tool.poetry.dev-dependencies]
pytest = "^8.0.0"
pytest-asyncio = "^0.26.0"
mypy = "^1.9.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
