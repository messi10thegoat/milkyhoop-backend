[tool.poetry]
name = "chatbot_service"
version = "0.1.0"
description = "MilkyHoop Chatbot Service - Direct Dependencies Only"
authors = ["MilkyHoop Team"]

[tool.poetry.dependencies]
python = "^3.10"

# Core Web Framework
fastapi = "^0.110.0"
uvicorn = {extras = ["standard"], version = "^0.29.0"}

# gRPC Communication
grpcio = "^1.71.0"
grpcio-tools = "^1.63.0"
grpcio-health-checking = "^1.63.0"
grpc-interceptor = "^0.15.0"
protobuf = "^5.29.0"

# AI & LLM Integration
openai = "^1.0.0"

# Database & ORM
prisma = "^0.15.0"

# Utilities & Reliability
tenacity = "^8.2.0"
httpx = "^0.27.0"

# Logging & Monitoring
structlog = "^25.2.0"
prometheus-client = "^0.20.0"

# Configuration
python-dotenv = "^1.0.0"

[tool.poetry.dev-dependencies]
pytest = "^8.0.0"
pytest-asyncio = "^0.26.0"
mypy = "^1.9.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
