[tool.poetry]
name = "milkyhoop-api-gateway"
version = "3.0.0"
description = "MilkyHoop API Gateway - Production Dependencies"
authors = ["MilkyHoop Dev <dev@milkyhoop.com>"]
packages = [{ include = "app" }]

[tool.poetry.dependencies]
python = "^3.11"

# Core FastAPI Stack
fastapi = "0.116.1"
uvicorn = {extras = ["standard"], version = "0.35.0"}
starlette = "0.47.3"
pydantic = "2.11.7"
pydantic-core = "2.33.2"

# HTTP & Networking
httpx = "0.28.1"
httpcore = "1.0.9"
requests = "2.32.5"
h11 = "0.16.0"
httptools = "0.6.4"
websockets = "15.0.1"

# gRPC Stack
grpcio = "1.74.0"
grpcio-tools = "1.74.0"
grpc-interceptor = "0.15.4"
protobuf = "6.32.0"

# Database & ORM
prisma = "0.15.0"

# AI & LLM
openai = "1.105.0"

# Redis & Caching
redis = "6.4.0"

# Monitoring & Observability
prometheus-client = "0.22.1"
structlog = "25.4.0"

# Utilities
python-dotenv = "1.1.1"
python-multipart = "0.0.20"
tenacity = "9.1.2"
typing-extensions = "4.15.0"
click = "8.2.1"

# Template & Config
Jinja2 = "3.1.6"
MarkupSafe = "3.0.2"
tomlkit = "0.13.3"
PyYAML = "6.0.2"

# Development & Build Tools
nodeenv = "1.9.1"
watchfiles = "1.1.0"
uvloop = "0.21.0"

# Security & Networking
certifi = "2025.8.3"
charset-normalizer = "3.4.3"
urllib3 = "2.5.0"
idna = "3.10"

# Async & Concurrency
anyio = "4.10.0"
sniffio = "1.3.1"

# Progress & UI
tqdm = "4.67.1"

# Type Checking & Annotations
annotated-types = "0.7.0"
typing-inspection = "0.4.1"

# System
distro = "1.9.0"
jiter = "0.10.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.dev-dependencies]
pytest = "^8.1.1"
