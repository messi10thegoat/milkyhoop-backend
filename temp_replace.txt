            <MessageBubble 
              message={{
                text: message.text,
                isFromUser: message.isUser,
                timestamp: message.timestamp.toLocaleTimeString('id-ID', { hour: '2-digit', minute: '2-digit' })
              }}
            />
