// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: queries.sql

package db

import (
	"context"

	"github.com/google/uuid"
)

const insertNotification = `-- name: InsertNotification :one
INSERT INTO notifications (user_id, message, type, status, created_at)
VALUES ($1, $2, $3, $4, NOW())
RETURNING id
`

type InsertNotificationParams struct {
	UserID  uuid.UUID `json:"user_id"`
	Message string    `json:"message"`
	Type    string    `json:"type"`
	Status  string    `json:"status"`
}

func (q *Queries) InsertNotification(ctx context.Context, arg InsertNotificationParams) (int32, error) {
	row := q.db.QueryRowContext(ctx, insertNotification,
		arg.UserID,
		arg.Message,
		arg.Type,
		arg.Status,
	)
	var id int32
	err := row.Scan(&id)
	return id, err
}

const listNotificationsByUser = `-- name: ListNotificationsByUser :many
SELECT id, user_id, message, type, status, created_at
FROM notifications
WHERE user_id = $1
ORDER BY created_at DESC
LIMIT 100
`

func (q *Queries) ListNotificationsByUser(ctx context.Context, userID uuid.UUID) ([]Notification, error) {
	rows, err := q.db.QueryContext(ctx, listNotificationsByUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Notification
	for rows.Next() {
		var i Notification
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Message,
			&i.Type,
			&i.Status,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
